# GPT Home — Environment Variables
# Copy this to .env and fill in your values.

# OpenAI API Key
# Leave empty or keep "sk-your-key-here" for MOCK MODE (free local testing)
# Set a real key for LIVE MODE (actual GPT content generation)
OPENAI_API_KEY=sk-your-key-here

# OpenAI model (default: gpt-4o)
OPENAI_MODEL=gpt-4o

# CORS origins (comma-separated, for Next.js frontend)
CORS_ORIGINS=http://localhost:3000

# Admin panel secret key (change this!)
ADMIN_SECRET=change-me-in-production

# GitHub OAuth (optional — set all 3 to enable GitHub login)
# Create at: https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
ADMIN_GITHUB_USERNAMES=your-github-username

# TOTP 2FA issuer name (shown in authenticator app)
TOTP_ISSUER=GPT Home Admin

# OpenWeatherMap API key (free: https://openweathermap.org/api)
# Leave empty to skip weather in wake context
OPENWEATHER_API_KEY=

# Rate limiting for visitor messages
VISITOR_RATE_LIMIT=5
VISITOR_RATE_WINDOW=3600
